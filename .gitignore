# 运行时产物 / 数据 (不上传)
data/
dashboard/dashboard_data.js
dashboard/history/
*.db
*.pkl
*.csv
*.xlsx

# Python
__pycache__/
*.pyc
.venv/
venv/

# 系统
.DS_Store
Thumbs.db

# docs/ 用于 GitHub Pages, 需上传
!docs/dashboard_data.js

# pre-core-merge backups (local only)
*/_pre_core_backup/
tvtarget/output/

# tvtarget 运行时快照: 每日重算的产物, 只有种子表 screen_seed.json 入 git
# (2026-09-07: history_snap/beat 曾被误 add 进仓库, 其中 screen_2026-08-31.json 还是
#  08-30 的逐字节副本 —— 假快照污染了回看; 已 git rm --cached 并在此钉死)
tvtarget/data/history_snap/
tvtarget/data/beat/
tvtarget/data/screen.json
!tvtarget/data/screen_seed.json
